{% macro i18n(content) %}
    {% if content[_lang] is defined %}
        {{ content[_lang] }}

    {% elseif content[_fallback] is defined %}
        {{ content[_fallback] }}
    {% else %}
        {{ _dict.get(content, _lang) }}
    {% endif %}
{% endmacro %}